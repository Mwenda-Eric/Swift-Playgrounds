import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack(alignment: .trailing) {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundColor(.blue)
                .opacity(34)
            Text("Hello, Eric!")
            Text("Hello, Mwenda!")
        }
    }
}
